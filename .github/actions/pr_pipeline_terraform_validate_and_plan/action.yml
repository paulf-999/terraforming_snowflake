name: "Terraform Validation"
description: "Validates and plans Terraform changes"
inputs:
  environment:
    required: true
    description: "The environment to validate"

runs:
  using: "composite"
  steps:

    - name: Terraform Validate
      run: |
        ./src/cicd/scripts/terraform_validate.sh ${{ inputs.environment }}
      shell: bash
