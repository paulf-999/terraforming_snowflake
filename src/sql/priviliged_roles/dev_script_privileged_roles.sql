-- Drop the database admin role if it exists
DROP ROLE IF EXISTS SNOWFLAKE_DB_ADMIN_ROLE;

-- Drop the user admin role if it exists
DROP ROLE IF EXISTS SNOWFLAKE_USER_ADMIN_ROLE;

-- Drop the role management admin role if it exists
DROP ROLE IF EXISTS SNOWFLAKE_ROLE_ADMIN_ROLE;

-- Drop the grant management admin role if it exists
DROP ROLE IF EXISTS SNOWFLAKE_GRANT_MGMT_ADMIN_ROLE;

-- Drop the warehouse admin role if it exists
DROP ROLE IF EXISTS SNOWFLAKE_WAREHOUSE_ADMIN_ROLE;

-- Drop the task admin role if it exists
DROP ROLE IF EXISTS SNOWFLAKE_TASK_ADMIN_ROLE;

-- Drop the tag admin role if it exists
DROP ROLE IF EXISTS SNOWFLAKE_TAG_ADMIN_ROLE;
DROP ROLE IF EXISTS SNOWFLAKE_STAGE_ADMIN_ROLE;
